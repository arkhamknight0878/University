#include<stdio.h>
#include"functions.h"

int main()
{
	unsigned position = 0;

	printf("Position of prime number = ");
	scanf_s("%u", &position);
	printf("Answer is %llu", n_element(position));

	return 0;
}